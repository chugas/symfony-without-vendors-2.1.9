{% for owner in hwi_oauth_resource_owners() %}
  {% set ownerName = owner|trans({}, 'HWIOAuthBundle') %}
  <a href="{{ hwi_oauth_login_url(owner) }}" class="{{ ownerName }} register" title="{{ ownerName }}">{{ ownerName | upper }}</a>
{% endfor %}
